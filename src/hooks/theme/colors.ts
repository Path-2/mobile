import { primary } from "../../configs/colors";

// Light theme colors
export const lightColors = {
  primary: {
    txt: "#111115",
    bg: "#fff",
    bgBt: primary,
    txtBt: '#fff'
  },
  secondary: {
    txt: primary,
    bg: primary,
    bgBt: primary,
    txtBt: '#fff'
  },
};

// Dark theme colors
export const darkColors = {
  primary: {
    txt: "#fff",
    bg: "#111115",
    bgBt: primary,
    txtBt: '#fff'
  },
  secondary: {
    txt: primary,
    bg: primary,
    bgBt: primary,
    txtBt: '#fff'
  },
};
